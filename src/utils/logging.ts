export function logInfo(message: string): void {
  // Minimal logger for early scaffolding; will be replaced by structured logging.
  // eslint-disable-next-line no-console
  console.log(`[bemadralphy] ${message}`);
}
