import { logInfo } from '../utils/logging.js';

export async function storiesToBeads(storyPaths: string[]): Promise<string[]> {
  logInfo(`Beads adapter not implemented yet (stories=${storyPaths.length})`);
  return [];
}
